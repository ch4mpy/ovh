# See https://github.com/bitnami/charts/tree/master/bitnami/postgresql

global:
  postgresql:
    auth:
      username: keycloak
      password: change-me
      database: keycloak
primary:
  persistence:
    existingClaim: postgresql-keycloak